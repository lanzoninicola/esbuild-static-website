(() => {
  // dist/index.js
  (() => {
    function multiply(a, b) {
      return a * b;
    }
    function sum2(a, b) {
      return a + b;
    }
    var SessionManager2 = class {
      constructor() {
      }
      set = (key, value) => {
        let _value = value;
        if (typeof _value === "object") {
          _value = JSON.stringify(_value);
        }
        sessionStorage.setItem(key, _value);
      };
      get = (key) => {
        const value = sessionStorage.getItem(key);
        if (value === "null") {
          return null;
        }
        if (value === "undefined") {
          return false;
        }
        if (value?.charAt(0) === "{") {
          return JSON.parse(value);
        }
        return value;
      };
      remove = (key) => {
        sessionStorage.removeItem(key);
      };
    };
  })();

  // pages/about/index.js
  var sessionManager = new (void 0)();
  var firstNumber = document.getElementById("firstNumber");
  var secondNumber = document.getElementById("secondNumber");
  var resultCell = document.getElementById("result");
  var btnResult = document.getElementById("makeSum");
  var sumResult = 0;
  btnResult.addEventListener("click", makeSum);
  function makeSum() {
    sumResult = (void 0)(parseInt(firstNumber.value, 10), parseInt(secondNumber.value, 10));
    resultCell.textContent = sumResult;
    sessionManager.set("sumResult", sumResult);
  }
})();
//# sourceMappingURL=index.min.js.map
